import styled from 'styled-components';
import { neutral } from '../../../utils';

export const StyledImg = styled.img`
  width: 117.8rem;
  border-radius: 12px;
  margin: 16.5rem auto 12.1rem auto;
  display: block;
`;

export const StyledDiv = styled.div`
  background-color: ${neutral[500]};
`;
